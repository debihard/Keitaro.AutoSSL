Автоматически добавляем https для всех доменов в Кейтаро
Рано или поздно всем арбитражникам приходится создавать SSL-сертификаты для своих доменов: кому-то кажется, что это добавит траста в фб, кто-то таким образом повышает доверие пользователей, а где-то (допустим, при прямых продажах) это прямое требование ПП.

Автоматически добавляем https для всех доменов в Кейтаро, изображение №1
Так какие же есть варианты подключения https для вашего ленда, чтобы увидеть заветный замочек в строке браузера?

Автоматически добавляем https для всех доменов в Кейтаро, изображение №2
Самым простым способом будет использование CloudFlare: добавляем туда наш домен и на вкладке SSL/TLS включаем режим Flexible.

Автоматически добавляем https для всех доменов в Кейтаро, изображение №3
Но добавлять каждый домен в CloudFlare не всегда бывает нужно + хотелось бы как-то один раз настроить подключение HTTPS и забыть про это, как про страшный сон.

Сегодня разберёмся с трекером Кейтаро и запилим скрипт, который будет генерировать SSL-сертификаты для всех припаркованных к нему доменов.

Сам по себе трекер не умеет генерировать сертификаты и по умолчанию процесс выглядит следующим образом:

Паркуем домен к трекеру и добавляем его на соответствующей вкладке
Через терминал соединяемся с сервером, на котором установлен трекер и запускаем скрипт генерирования сертификата
После того, как сертификат выпущен, включаем галочку HTTPS у домена
Автоматически добавляем https для всех доменов в Кейтаро, изображение №4
Ну а теперь представим, что домены мы добавляем каждый день и не по одному или по два, а по 22… 🤔

А теперь берём, скачиваем вот отсюда 3 файлика и сохраняем их себе в папку на сервере с трекером: в /var/www/ssl_cron

Открываем в любом текстовом редакторе оба php файла и заменяем там текст:

{YOUR_DOMAIN} меняем на домен или ip-адрес, на котором у нас висит трекер

{YOUR_API_KEY} меняем на ключ доступа к API Кейтаро

Теперь нам надо настроить планировщик задач Cron, чтобы каждую минуту запускался скрипт, который будет брать рандомный домен БЕЗ https и выпускать для него сертификат:

Подключаемся через терминал к серверу
Пишем crontab -e и попадаем в редактор
Добавляем в конец вот такую строку:
*/1 * * * * sudo -u root /var/www/ssl_cron/ssl.sh

Всё! Теперь для каждого припаркованного домена в трекере максимум через минуту будет выпущен сертификат и включена галочка HTTPS.

А с вами был Жёлтый веб, лейте в плюс, господа! ☝
